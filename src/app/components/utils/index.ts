import { Input, LocalOptions } from "./inputs";
import { Frame, Container, Content, Group } from "./tags";

export const Tag = {
  container: Container,
  frame: Frame,
  content: Content,
  input: Input,
  option: LocalOptions,
  group: Group,
};

